# API Configuration
API_KEY=api_key_here
CACHE_TTL=3600  # Cache TTL in seconds (default: 1 hour)

# Model Configuration
MODEL_PATH=models/news_classifier_logistic.joblib
VECTORIZER_PATH=models/vectorizer.joblib
MODEL_NAME=news_classifier_logistic
MODEL_VERSION=1

# API Server Configuration
HOST=0.0.0.0
PORT=7860
WORKERS=4

# MLflow Configuration
MLFLOW_TRACKING_URI=http://localhost:5000
MLFLOW_EXPERIMENT_NAME=news-classification

# Kaggle API Configuration
KAGGLE_USERNAME=kaggle_username_here
KAGGLE_API_KEY=Kaggle_api_here

# Docker Configuration (if using Docker)
DOCKER_PORT=7860

# Production Deployment Configuration
# Domain name for your deployment (e.g., yourdomain.com)
DOMAIN=localhost

# Email for Let's Encrypt certificate generation
ACME_EMAIL=test@example.com

# Traefik dashboard authentication (username:password in bcrypt format)
# Generate with: htpasswd -nb admin your_password
# For testing: admin:admin (password: admin)
TRAEFIK_AUTH=admin:$$2y$$10$$DX8gKy3JzO/ZL.3LJ7J2/.u5Xq5PKl1K5qh2L2tK5kL1Y1Y3L9L12

# Service Ports (for local development only)
API_PORT=7860
GRAFANA_PORT=3000
MLFLOW_PORT=5000
PREFECT_PORT=4200
LOCUST_PORT=8089
PROMETHEUS_PORT=9090

# Grafana Configuration
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=admin
GF_USERS_ALLOW_SIGN_UP=false

# Prefect Configuration
RUN_PIPELINE=true

# Locust Configuration
LOCUST_MODE=standalone
LOCUST_USERS=100
LOCUST_SPAWN_RATE=10
